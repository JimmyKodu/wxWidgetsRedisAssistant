#!/bin/bash
# Launcher script template for Redis Assistant
# This script sets up the library path and launches the application

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export LD_LIBRARY_PATH="$DIR:$LD_LIBRARY_PATH"
"$DIR/RedisAssistant" "$@"
